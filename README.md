# React Todo

### Installtion
1. Install node dependencies
   ```
   npm install
   ```
2. Start the project
   ```
   npm run dev
   ```

   Navigate to (localhost:5173)[http://localhost:5173] in the browser
